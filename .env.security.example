# Security Configuration Example
# This file contains SECURE configuration examples
# Do NOT use these values in production

# Database Configuration
DATABASE_URL=postgresql://username:password@localhost/brainforge

# Security Keys (Generate new ones for production)
SECRET_KEY=generate-secure-key-32-chars-minimum
ENCRYPTION_KEY=generate-encryption-key-32-chars

# API Keys (Replace with actual keys)
OPENAI_API_KEY=your-openai-api-key-here
GOOGLE_SEARCH_API_KEY=your-google-search-api-key-here
NEWS_API_KEY=your-news-api-key-here

# Database Credentials (Use environment variables in production)
POSTGRES_DB=brainforge
POSTGRES_USER=brainforge_user
POSTGRES_PASSWORD=SECURE_PASSWORD_HERE

# Security Settings
JWT_EXPIRE_MINUTES=30
ALLOWED_ORIGINS=http://localhost:8000

# ⚠️  SECURITY WARNING ⚠️
# 
# 1. NEVER commit actual credentials to version control
# 2. Use environment variables or secret management systems
# 3. Generate unique secret keys for each deployment
# 4. Rotate keys regularly (every 90 days recommended)
# 5. Use strong passwords (minimum 12 characters, mixed case, numbers, symbols)
# 
# For production deployments, use:
# - Kubernetes secrets
# - AWS Parameter Store / Secrets Manager
# - Azure Key Vault
# - HashiCorp Vault
# - Docker secrets
# - Environment variable injection

# Example of secure key generation:
# openssl rand -hex 32  # For SECRET_KEY
# openssl rand -hex 32  # For ENCRYPTION_KEY